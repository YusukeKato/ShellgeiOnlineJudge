メンバーのリストが入力として与えられるので、各メンバーをチームごとに並べてください。
入力の一列目はメンバーの名前、二列目はチーム番号です。
出力の一列目はチーム番号、二列目以降はメンバーの名前としてください。
また、メンバーの名前はリストで上になっている人が先にくるように並べてください。
Grouping each member by team.

Input:
- first row: the member's name
- second row: the team number

Output:
- first row: the team number
- second row: the member's name
